# Head & Tail

Sometimes files are too large to read comfortably with `cat`. The `head` command shows you the beginning of a file, and the `tail` command shows you the end.

In this directory, there is a file called `large_file.txt` with 1001 lines. The flag is on the very last line. Use the right tool to find it!

## Allowed commands:

* head
* tail
